# 🤖 Telegram Бот для Мониторинга Сервера

Мощный бот для мониторинга Ubuntu сервера с уведомлениями о важных событиях в реальном времени.

## ✨ Возможности

### 📊 Мониторинг системы
- **CPU** - общая загрузка и по каждому ядру
- **RAM** - использование оперативной памяти и SWAP
- **Диски** - занятое и свободное место на всех разделах
- **Процессы** - топ-10 процессов по загрузке CPU
- **Сеть** - статистика трафика и активных соединений
- **Uptime** - время работы сервера

### 🔧 Мониторинг сервисов
- Отслеживание статуса systemd сервисов
- Автоматические уведомления при:
  - Падении сервиса
  - Перезапуске сервиса
  - Изменении состояния
- Просмотр логов любого сервиса

### 🚨 Умные уведомления
- Предупреждения о заполнении дисков (>85%)
- Уведомления о перезагрузке сервера
- Мониторинг важных событий в реальном времени

### 🔐 Безопасность
- Whitelist пользователей - доступ только авторизованным
- Все команды логируются
- Защита от несанкционированного доступа

## 🚀 Быстрый старт

### 1. Создание бота в Telegram

1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота
4. Сохраните токен - он понадобится для настройки

### 2. Узнайте свой Telegram ID

1. Напишите [@userinfobot](https://t.me/userinfobot)
2. Бот пришлёт вам ваш ID (например, `123456789`)
3. Сохраните ID - он понадобится для whitelist

### 3. Установка на сервере

```bash
# Клонируйте репозиторий или скопируйте файлы
cd /opt
git clone <your-repo> tg-servmonitoringbot
cd tg-servmonitoringbot

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 4. Настройка

Скопируйте файл с примером конфигурации:
```bash
cp .env.example .env
```

Отредактируйте `.env` файл:
```bash
nano .env
```

Заполните следующие параметры:

```env
# Токен бота от @BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# ID пользователей, которым разрешён доступ (через запятую)
ALLOWED_USERS=123456789,987654321

# Сервисы для мониторинга (через запятую)
MONITORED_SERVICES=nginx,postgresql,redis

# Интервалы проверок (в секундах)
SERVICE_CHECK_INTERVAL=60
DISK_CHECK_INTERVAL=300
UPTIME_CHECK_INTERVAL=30

# Порог предупреждения о заполнении диска (%)
DISK_WARNING_THRESHOLD=85
```

### 5. Запуск

#### Ручной запуск (для тестирования)
```bash
source venv/bin/activate
python bot.py
```

#### Запуск через systemd (рекомендуется)

Создайте systemd сервис:
```bash
sudo nano /etc/systemd/system/tg-monitor-bot.service
```

Вставьте следующее содержимое (замените `YOUR_USER` на вашего пользователя):
```ini
[Unit]
Description=Telegram Server Monitoring Bot
After=network.target

[Service]
Type=simple
User=YOUR_USER
WorkingDirectory=/opt/tg-servmonitoringbot
Environment="PATH=/opt/tg-servmonitoringbot/venv/bin"
ExecStart=/opt/tg-servmonitoringbot/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустите сервис:
```bash
# Перезагрузите systemd
sudo systemctl daemon-reload

# Включите автозапуск
sudo systemctl enable tg-monitor-bot

# Запустите бота
sudo systemctl start tg-monitor-bot

# Проверьте статус
sudo systemctl status tg-monitor-bot

# Просмотр логов
sudo journalctl -u tg-monitor-bot -f
```

## 📱 Использование

### Доступные команды

#### Системная информация
- `/status` - общий обзор системы (CPU, RAM, диски, uptime)
- `/cpu` - детальная информация о процессоре
- `/memory` - информация о памяти и SWAP
- `/disk` - информация о всех дисках
- `/processes` - топ-10 процессов по загрузке CPU
- `/network` - сетевая статистика

#### Мониторинг сервисов
- `/services` - статус всех отслеживаемых сервисов
- `/logs nginx` - последние 20 строк логов сервиса nginx
- `/logs postgresql` - логи сервиса postgresql

#### Справка
- `/help` - список всех команд
- `/about` - информация о боте

## 🔧 Настройка сервисов для мониторинга

### Примеры популярных сервисов

**Веб-серверы:**
```env
MONITORED_SERVICES=nginx,apache2
```

**Python приложения:**
```env
MONITORED_SERVICES=gunicorn,uvicorn,celery
```

**Ruby приложения:**
```env
MONITORED_SERVICES=puma,unicorn,sidekiq
```

**Базы данных:**
```env
MONITORED_SERVICES=postgresql,mysql,redis,mongodb
```

**Docker:**
```env
MONITORED_SERVICES=docker
```

**Комбинированный пример:**
```env
MONITORED_SERVICES=nginx,postgresql,redis,gunicorn,celery
```

### Как узнать имя сервиса?

```bash
# Посмотреть все запущенные сервисы
systemctl list-units --type=service --state=running

# Проверить конкретный сервис
systemctl status nginx
```

## 🎯 Примеры уведомлений

### Падение сервиса
```
⚠️ Сервис остановлен!

🔴 nginx
Состояние: inactive/dead
Время: 14:35:22
```

### Перезапуск сервиса
```
🔄 Сервис перезапущен!

🟡 postgresql
Старый PID: 1234 → Новый PID: 5678
Время: 14:35:22
```

### Заполнение диска
```
⚠️ Предупреждение о дисковом пространстве!

🔴 Диск / заполнен на 87.3%
Свободно: 12.5 GB из 100 GB

Рекомендуется освободить место!
```

### Перезагрузка сервера
```
🔄 Обнаружена перезагрузка сервера!

⏱ Время перезагрузки: 2026-01-18 14:30:00
⏰ Текущее время: 14:32:15
```

## 🛠 Устранение проблем

### Бот не отвечает
```bash
# Проверьте статус
sudo systemctl status tg-monitor-bot

# Посмотрите логи
sudo journalctl -u tg-monitor-bot -n 50
```

### Ошибка доступа к сервисам
```bash
# Убедитесь, что пользователь может читать логи
sudo usermod -aG systemd-journal YOUR_USER

# Перезайдите в систему для применения изменений
```

### Бот не видит сервис
```bash
# Проверьте, что сервис существует
systemctl list-units --type=service | grep your-service

# Проверьте правильность имени в .env
nano .env
```

## 📝 Обновление

```bash
cd /opt/tg-servmonitoringbot
git pull
source venv/bin/activate
pip install -r requirements.txt --upgrade
sudo systemctl restart tg-monitor-bot
```

## 🔒 Безопасность

1. **Никогда не публикуйте** файл `.env` - он содержит токен бота
2. **Добавьте в whitelist** только доверенных пользователей
3. **Регулярно проверяйте** логи бота на подозрительную активность
4. **Используйте сильные токены** от BotFather

## 📊 Производительность

- Потребление RAM: ~30-50 MB
- Потребление CPU: <1% (в режиме ожидания)
- Частота проверок: настраивается (по умолчанию 60 сек для сервисов)

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи: `sudo journalctl -u tg-monitor-bot -f`
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность конфигурации в `.env`
4. Убедитесь, что бот имеет доступ к systemctl

## 📄 Лицензия

MIT License

## 🎉 Возможности для расширения

В будущих версиях планируется добавить:
- Управление сервисами через бота (перезапуск, остановка)
- Графики загрузки системы
- Экспорт метрик в Prometheus
- Интеграция с другими системами мониторинга
- Настраиваемые алерты
- Поддержка Docker контейнеров

---

**Приятного использования! 🚀**

