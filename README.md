# 🤖 Telegram Бот для Мониторинга Сервера

Мощный бот для мониторинга Ubuntu сервера с уведомлениями о важных событиях в реальном времени.

## ✨ Возможности

### 📊 Мониторинг системы
- **Status** — краткий обзор CPU/RAM/дисков/uptime
- **Network** — детальная статистика по интерфейсам
- **Process** — 10 самых тяжёлых процессов

### 🔐 Безопасность
- Whitelist пользователей - доступ только авторизованным
- Все команды логируются
- Защита от несанкционированного доступа

## 🚀 Быстрый старт (кратко)

1) Получите токен у [@BotFather](https://t.me/BotFather) и свой ID у [@userinfobot](https://t.me/userinfobot).

2) Установите зависимости:
```bash
cd /opt
git clone <your-repo> tg-servmonitoringbot
cd tg-servmonitoringbot
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

3) Создайте `.env`:
```env
BOT_TOKEN=ваш_токен_от_BotFather
ALLOWED_USERS=ваш_telegram_id
```

4) Запуск:
```bash
python bot.py
```

## 📱 Использование

### Доступные команды

#### Системная информация
- `/status` - общий обзор системы (CPU, RAM, диски, uptime)
- `/network` - детальная сетевая статистика
- `/process` - топ-10 самых тяжёлых процессов

## 🛠 Устранение проблем

### Бот не отвечает
```bash
# Проверьте статус
sudo systemctl status tg-monitor-bot

# Посмотрите логи
sudo journalctl -u tg-monitor-bot -n 50
```

## 📝 Обновление

```bash
cd /opt/tg-servmonitoringbot
git pull
source venv/bin/activate
pip install -r requirements.txt --upgrade
sudo systemctl restart tg-monitor-bot
```

## 🔒 Безопасность

1. **Никогда не публикуйте** файл `.env` - он содержит токен бота
2. **Добавьте в whitelist** только доверенных пользователей
3. **Регулярно проверяйте** логи бота на подозрительную активность
4. **Используйте сильные токены** от BotFather

## 📊 Производительность

- Потребление RAM: ~30-50 MB
- Потребление CPU: <1% (в режиме ожидания)

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи: `sudo journalctl -u tg-monitor-bot -f`
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность конфигурации в `.env`

## 📄 Лицензия

MIT License

## 🎉 Возможности для расширения

В будущих версиях планируется добавить:
- Графики загрузки системы
- Экспорт метрик в Prometheus
- Интеграция с другими системами мониторинга
- Настраиваемые алерты
- Поддержка Docker контейнеров

---

**Приятного использования! 🚀**

